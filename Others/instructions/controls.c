#include <stdio.h>

// pour affectation =, automatique conversion
// renvoie la valeur depose dans la variable 
// if (x = -1) always true

// here we got j = ++i and j = i++ different cases


// bit operations
// (~, &, |, ^) , do not confuse with && and ||

// le cast operator (int)(i/j)
// nous avons l'operator le plus fort et plus faible
// le resultat est toujours le plus fort. 


